services:
  app:
    image: aikio/express-app:latest
    ports:
      - "8080:8080"

  updater:
    image: containrrr/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 30
    depends_on:
      - app